MONGO_URI=mongodb+srv://USERNAME:PASSWORD@cluster0.xxxxx.mongodb.net/venue_crm?retryWrites=true&w=majority&authSource=admin
PORT=5000
NODE_ENV=development
JWT_SECRET=your_super_secret_jwt_key
JWT_EXPIRE=7d

# Extra allowed origins (comma-separated). Vercel prod URL is built-in.
# CORS_ORIGIN=https://venue-crm-ten.vercel.app,http://localhost:3000
